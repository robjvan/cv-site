export interface ISkillMetric {
  /** Name or title of the skill being measured. */
  title: string;

  /** Proficiency level of the skill, represented as a number from 0 to 10. */
  value: number;
}
