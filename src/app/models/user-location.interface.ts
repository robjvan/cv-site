export interface IUserLocation {
  /** Geographic latitude of the user's location, in decimal degrees. */
  lat: number;

  /** Geographic longitude of the user's location, in decimal degrees. */
  lon: number;
}
