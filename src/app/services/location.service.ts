import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class LocationService {
  constructor() {}

  getUserLocation(ip: string) {
    // TODO(RV): Add logic to get user location by IP
    // Use location to show local weather data
  }
}
