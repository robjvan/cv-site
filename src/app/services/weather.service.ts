import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class WeatherService {
  constructor() {}

  fetchWeatherData() {
    // TODO(RV): Add logic to fetch weather data
  }
}
